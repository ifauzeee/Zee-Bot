

services:
  bot:
    build: .
    container_name: zee-ubot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - TZ=Asia/Jakarta
    volumes:
      - ./session:/app/session
    stdin_open: true # docker run -i
    tty: true        # docker run -t
